const AsyncHandler = (fun) => async (req, res, next) => {
  try {
    fun(req, res, next);
  } catch (error) {
    res.status(error.code || 500).json({
      message: error.message,
      success: false,
    });
  }
};

export { AsyncHandler };
